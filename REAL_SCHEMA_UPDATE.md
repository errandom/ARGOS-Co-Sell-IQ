# Real Schema Update

The project documentation and setup are now aligned to your shared Fabric schema.

## Confirmed Model

- Existing tables are queried as source data.
- No table creation/migration is required.
- Query patterns are based on `dbo.MSX_accounts`, `dbo.MSX_opportunities`, `dbo.MSX_opportunitydealteam`, and `dbo.MSX_partnerreferrals`.

Use `MSX_SCHEMA_REFERENCE.md` for column-level details and `FABRIC_SETUP.md` for end-to-end setup.
